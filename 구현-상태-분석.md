# 가상돔 기반 렌더링 시스템 구현 상태 분석

## 목차
1. [가상돔을 기반으로 렌더링하기](#가상돔을-기반으로-렌더링하기)
2. [이벤트 위임](#이벤트-위임)
3. [Diff 알고리즘 구현](#diff-알고리즘-구현)

---

## 가상돔을 기반으로 렌더링하기

### ✅ createVNode 함수를 이용하여 vNode를 만든다

**구현 위치**: `src/lib/createVNode.js`

```1:16:src/lib/createVNode.js
export function createVNode(type, props, ...children) {
  const flattened = children.flat(Infinity);
  const filtered = flattened.filter(
    (child) =>
      child !== null &&
      child !== undefined &&
      child !== false &&
      child !== true,
  );

  return {
    type: type,
    props: props,
    children: filtered,
  };
}
```

**구현 내용**:
- `type`, `props`, `children`을 받아서 vNode 객체를 생성합니다.
- `children`을 `flat(Infinity)`로 평탄화하여 중첩 배열을 처리합니다.
- `null`, `undefined`, `false`, `true` 같은 falsy 값을 필터링합니다.
- JSX 변환을 위해 `render.jsx`에서 `/** @jsx createVNode */` 주석으로 JSX 변환기를 지정합니다.

**사용 예시**:
```jsx
/** @jsx createVNode */
const vNode = <div className="container">Hello</div>;
// 위 JSX는 createVNode('div', { className: 'container' }, 'Hello')로 변환됩니다.
```

---

### ✅ normalizeVNode 함수를 이용하여 vNode를 정규화한다

**구현 위치**: `src/lib/normalizeVNode.js`

```1:49:src/lib/normalizeVNode.js
export function normalizeVNode(vNode) {
  // null, undefined, true, false는 빈 문자열로 변환
  if (
    vNode === null ||
    vNode === undefined ||
    vNode === true ||
    vNode === false
  ) {
    return "";
  }

  // 문자열과 숫자는 문자열로 변환
  if (typeof vNode === "string" || typeof vNode === "number") {
    return String(vNode);
  }

  // VNode 객체인지 확인 (type 속성이 있는 객체)
  if (typeof vNode === "object" && vNode !== null && "type" in vNode) {
    const { type, props, children } = vNode;

    // type이 함수면 컴포넌트 실행
    if (typeof type === "function") {
      const componentProps = { ...(props || {}), children };
      const componentResult = type(componentProps);
      return normalizeVNode(componentResult);
    }

    // 일반 요소인 경우, 자식들을 재귀적으로 정규화하고 falsy 값 제거
    const normalizedChildren = (children || [])
      .map((child) => normalizeVNode(child))
      .filter(
        (child) =>
          child !== null &&
          child !== undefined &&
          child !== false &&
          child !== true &&
          child !== "",
      );

    return {
      type,
      props: props || null,
      children: normalizedChildren,
    };
  }

  // 그 외의 경우는 그대로 반환
  return vNode;
}
```

**구현 내용**:
- **Falsy 값 처리**: `null`, `undefined`, `true`, `false`를 빈 문자열로 변환합니다.
- **원시 타입 처리**: 문자열과 숫자를 문자열로 변환합니다.
- **컴포넌트 실행**: `type`이 함수인 경우 컴포넌트로 인식하여 실행하고, 결과를 재귀적으로 정규화합니다.
- **자식 정규화**: 자식 노드들을 재귀적으로 정규화하고, falsy 값과 빈 문자열을 제거합니다.
- **일관된 구조**: 모든 vNode를 일관된 구조로 변환하여 이후 처리 단계에서 안정적으로 동작하도록 합니다.

**핵심 기능**:
- 함수형 컴포넌트를 실행하여 실제 DOM 요소로 변환 가능한 vNode로 만듭니다.
- 중첩된 컴포넌트도 재귀적으로 처리합니다.

---

### ✅ createElement 함수를 이용하여 vNode를 실제 DOM으로 만든다

**구현 위치**: `src/lib/createElement.js`

```3:65:src/lib/createElement.js
export function createElement(vNode) {
  // null, undefined, true, false는 빈 텍스트 노드로 변환
  if (
    vNode === null ||
    vNode === undefined ||
    vNode === true ||
    vNode === false
  ) {
    return document.createTextNode("");
  }

  // 문자열과 숫자는 텍스트 노드로 변환
  if (typeof vNode === "string" || typeof vNode === "number") {
    return document.createTextNode(String(vNode));
  }

  // 배열은 DocumentFragment로 변환
  if (Array.isArray(vNode)) {
    const fragment = document.createDocumentFragment();
    vNode.forEach((child) => {
      const element = createElement(child);
      if (element) {
        fragment.appendChild(element);
      }
    });
    return fragment;
  }

  // VNode 객체인지 확인
  if (typeof vNode === "object" && vNode !== null && "type" in vNode) {
    const { type, props, children } = vNode;

    // type이 함수면 에러 발생
    if (typeof type === "function") {
      throw new Error("Component must be normalized before creating element");
    }

    // DOM 요소 생성
    const element = document.createElement(type);

    // 속성 설정
    if (props) {
      updateAttributes(element, props);
    }

    // 자식 요소 처리
    if (children && Array.isArray(children)) {
      children.forEach((child) => {
        if (child !== null && child !== undefined) {
          const childElement = createElement(child);
          if (childElement) {
            element.appendChild(childElement);
          }
        }
      });
    }

    return element;
  }

  // 그 외의 경우는 null 반환
  return null;
}
```

**속성 처리 함수** (`updateAttributes`):

```67:132:src/lib/createElement.js
function updateAttributes($el, props) {
  if (!props) return;

  Object.keys(props).forEach((key) => {
    const value = props[key];

    // 이벤트 핸들러 처리 (onClick, onMouseOver 등)
    if (key.startsWith("on") && typeof value === "function") {
      const eventType = key.slice(2).toLowerCase(); // onClick -> click
      addEvent($el, eventType, value);
      return;
    }

    // className을 class로 변환
    if (key === "className") {
      $el.setAttribute("class", value || "");
      return;
    }

    // 불리언 속성 처리
    if (typeof value === "boolean") {
      // checked와 selected는 DOM 속성 없이 property만 사용
      const isPropertyOnly = key === "checked" || key === "selected";

      if (value) {
        if (isPropertyOnly) {
          // property만 설정, DOM 속성은 설정하지 않음
          if (key in $el) {
            $el[key] = true;
          }
        } else {
          // 일반 boolean 속성은 DOM 속성도 설정
          $el.setAttribute(key, "");
          if (key in $el) {
            $el[key] = true;
          }
        }
      } else {
        // false일 때는 항상 제거
        $el.removeAttribute(key);
        if (key in $el) {
          $el[key] = false;
        }
      }
      return;
    }

    // data-* 속성은 dataset으로 처리
    if (key.startsWith("data-")) {
      const dataKey = key
        .slice(5)
        .replace(/-([a-z])/g, (_, letter) => letter.toUpperCase());
      $el.dataset[dataKey] = value;
      return;
    }

    // 일반 속성 설정
    if (value !== null && value !== undefined) {
      $el.setAttribute(key, value);
      // DOM 속성도 설정 (id, href 등)
      if (key in $el) {
        $el[key] = value;
      }
    }
  });
}
```

**구현 내용**:
- **Falsy 값 처리**: `null`, `undefined`, `true`, `false`를 빈 텍스트 노드로 변환합니다.
- **텍스트 노드 생성**: 문자열과 숫자를 텍스트 노드로 변환합니다.
- **배열 처리**: 배열을 `DocumentFragment`로 변환하여 여러 요소를 한 번에 추가할 수 있게 합니다.
- **DOM 요소 생성**: vNode의 `type`을 사용하여 실제 DOM 요소를 생성합니다.
- **속성 설정**: `updateAttributes` 함수를 통해 다양한 속성을 처리합니다:
  - 이벤트 핸들러: `onClick` → `click` 이벤트로 변환하여 이벤트 위임 시스템에 등록
  - `className` → `class` 속성으로 변환
  - 불리언 속성: `checked`, `selected` 등은 property만 설정
  - `data-*` 속성: `dataset` API 사용
  - 일반 속성: `setAttribute`와 property 모두 설정
- **자식 요소 재귀 처리**: 자식 vNode들을 재귀적으로 DOM 요소로 변환하여 추가합니다.
- **컴포넌트 보호**: 정규화되지 않은 컴포넌트(함수 타입)가 들어오면 에러를 발생시킵니다.

---

### ✅ 결과적으로, JSX를 실제 DOM으로 변환할 수 있도록 만들었다

**전체 플로우**:

```1:24:src/render.jsx
/** @jsx createVNode */
import { createVNode, renderElement } from "./lib";
import { cartStore, productStore, uiStore } from "./stores";
import { router } from "./router";
import { HomePage, NotFoundPage, ProductDetailPage } from "./pages";
import { withBatch } from "./utils";

// 홈 페이지 (상품 목록)
router.addRoute("/", HomePage);
router.addRoute("/product/:id", ProductDetailPage);
router.addRoute(".*", NotFoundPage);

/**
 * 전체 애플리케이션 렌더링
 */
export const render = withBatch(() => {
  const rootElement = document.getElementById("root");
  if (!rootElement) return;

  const PageComponent = router.target;

  // App 컴포넌트 렌더링
  renderElement(<PageComponent />, rootElement);
});
```

**렌더링 프로세스** (`renderElement`):

```9:51:src/lib/renderElement.js
export function renderElement(vNode, container) {
  // 최초 렌더링시에는 createElement로 DOM을 생성하고
  // 이후에는 updateElement로 기존 DOM을 업데이트한다.
  // 렌더링이 완료되면 container에 이벤트를 등록한다.

  if (!container) return;

  // vNode 정규화 (컴포넌트 실행)
  const normalizedVNode = normalizeVNode(vNode);

  // 이전 vNode 가져오기
  const oldVNode = containerVNodes.get(container);

  if (!oldVNode) {
    // 최초 렌더링: createElement로 DOM 생성
    const element = createElement(normalizedVNode);

    // container 비우기
    container.innerHTML = "";

    // DOM 추가
    if (element) {
      if (element instanceof DocumentFragment) {
        container.appendChild(element);
      } else {
        container.appendChild(element);
      }
    }
  } else {
    // 업데이트: updateElement로 DOM 업데이트
    updateElement(container, normalizedVNode, oldVNode, 0);
  }

  // 이전 vNode 저장
  containerVNodes.set(container, normalizedVNode);

  // container의 모든 자식 요소에 대해 container를 root로 설정
  migrateRootToContainer(container);

  // 이벤트 리스너 설정
  // container를 root로 사용하도록 설정
  setupEventListeners(container);
}
```

**전체 변환 과정**:
1. **JSX 작성**: `<PageComponent />` 형태로 JSX 작성
2. **JSX 변환**: `/** @jsx createVNode */` 주석에 의해 `createVNode(PageComponent, null)`로 변환
3. **vNode 생성**: `createVNode` 함수가 vNode 객체 생성
4. **정규화**: `normalizeVNode`가 컴포넌트를 실행하고 vNode 구조 정규화
5. **DOM 생성**: `createElement`가 vNode를 실제 DOM 요소로 변환
6. **렌더링**: `renderElement`가 DOM을 container에 추가

---

## 이벤트 위임

### ✅ 노드를 생성할 때 이벤트를 직접 등록하는게 아니라 이벤트 위임 방식으로 등록해야 한다

**구현 위치**: `src/lib/eventManager.js`

**핵심 메커니즘**:

```13:62:src/lib/eventManager.js
export function setupEventListeners(root) {
  if (!root) return;

  // root에 등록된 이벤트 타입들을 가져옴
  const eventTypes = rootEventTypes.get(root);
  if (!eventTypes || eventTypes.size === 0) return;

  // root의 위임 핸들러 맵 가져오기 또는 생성
  let delegatedHandlers = rootDelegatedHandlers.get(root);
  if (!delegatedHandlers) {
    delegatedHandlers = new Map();
    rootDelegatedHandlers.set(root, delegatedHandlers);
  }

  // 각 이벤트 타입에 대해 위임 핸들러 등록
  eventTypes.forEach((eventType) => {
    // 이미 등록된 핸들러가 있다면 중복 등록 방지
    if (delegatedHandlers.has(eventType)) return;

    const delegatedHandler = (e) => {
      // 이벤트가 발생한 타겟부터 root까지 올라가면서 핸들러 찾기
      let target = e.target;
      const path = [];

      // 이벤트 경로 수집 (target부터 root까지)
      while (target && target !== root.parentNode) {
        path.push(target);
        if (target === root) break;
        target = target.parentNode;
      }

      // 경로를 따라가면서 등록된 핸들러 찾아서 실행
      for (const element of path) {
        const handlersMap = eventHandlers.get(element);
        if (handlersMap) {
          const handlers = handlersMap.get(eventType);
          if (handlers) {
            handlers.forEach((handler) => {
              handler(e);
            });
          }
        }
      }
    };

    // bubble phase에서 실행 (기본값, capture phase가 아님)
    root.addEventListener(eventType, delegatedHandler);
    delegatedHandlers.set(eventType, delegatedHandler);
  });
}
```

**이벤트 등록 함수**:

```64:105:src/lib/eventManager.js
export function addEvent(element, eventType, handler) {
  if (!element || !eventType || !handler) return;

  // element의 핸들러 맵 가져오기 또는 생성
  let handlersMap = eventHandlers.get(element);
  if (!handlersMap) {
    handlersMap = new Map();
    eventHandlers.set(element, handlersMap);
  }

  // 해당 이벤트 타입의 핸들러 Set 가져오기 또는 생성
  let handlers = handlersMap.get(eventType);
  if (!handlers) {
    handlers = new Set();
    handlersMap.set(eventType, handlers);
  }

  // 핸들러 추가
  handlers.add(handler);

  // root 요소 찾기 (container)
  // element부터 시작해서 document.body의 직접 자식인 요소를 root로 사용
  let root = element;
  while (root.parentNode) {
    const parent = root.parentNode;
    // document.body의 직접 자식인 경우
    if (parent === document.body || parent === document) {
      // root는 document.body의 직접 자식인 요소 (현재 root)
      break;
    }
    // document.body나 document가 아니면 계속 올라감
    root = parent;
  }

  // root에 이벤트 타입 등록
  let eventTypes = rootEventTypes.get(root);
  if (!eventTypes) {
    eventTypes = new Set();
    rootEventTypes.set(root, eventTypes);
  }
  eventTypes.add(eventType);
}
```

**구현 내용**:
- **이벤트 핸들러 저장**: `WeakMap`을 사용하여 각 요소에 등록된 이벤트 핸들러를 저장합니다.
  - 구조: `Map<element, Map<eventType, Set<handler>>>`
- **Root 요소에 위임**: 각 요소의 이벤트 핸들러를 root 요소(container)에 위임 핸들러로 등록합니다.
- **이벤트 버블링 활용**: 이벤트가 발생하면 `e.target`부터 root까지의 경로를 따라가며 등록된 핸들러를 찾아 실행합니다.
- **중복 등록 방지**: 같은 root와 이벤트 타입에 대해 위임 핸들러를 한 번만 등록합니다.
- **직접 등록 없음**: 각 요소에 직접 `addEventListener`를 호출하지 않고, root에만 등록합니다.

**데이터 구조**:
```javascript
// 각 요소의 이벤트 핸들러 저장
eventHandlers: WeakMap<Element, Map<eventType, Set<handler>>>

// 각 root에 등록된 이벤트 타입 추적
rootEventTypes: WeakMap<Element, Set<eventType>>

// root에 등록된 위임 핸들러 저장
rootDelegatedHandlers: WeakMap<Element, Map<eventType, delegatedHandler>>
```

---

### ✅ 동적으로 추가된 요소에도 이벤트가 정상적으로 작동해야 한다

**구현 내용**:
- 이벤트 위임 방식을 사용하므로, 동적으로 추가된 요소에도 자동으로 이벤트가 작동합니다.
- 새로운 요소가 추가되면 `addEvent` 함수가 호출되어 해당 요소의 핸들러가 `eventHandlers` WeakMap에 저장됩니다.
- 실제 이벤트는 root 요소에서 처리되므로, 요소가 언제 추가되었는지와 관계없이 정상 작동합니다.

**동적 요소 처리 예시**:
```javascript
// 기존 요소
const button1 = document.createElement('button');
addEvent(button1, 'click', () => console.log('Button 1 clicked'));

// 나중에 동적으로 추가된 요소
const button2 = document.createElement('button');
addEvent(button2, 'click', () => console.log('Button 2 clicked'));
// button2도 root의 위임 핸들러를 통해 자동으로 작동합니다.
```

**마이그레이션 함수**:

```131:165:src/lib/eventManager.js
// container의 모든 자식 요소에 대해 container를 root로 설정
export function migrateRootToContainer(container) {
  if (!container) return;

  // container의 모든 자식 요소를 순회
  const walker = document.createTreeWalker(
    container,
    NodeFilter.SHOW_ELEMENT,
    null,
  );

  const eventTypesSet = new Set();
  let node;
  while ((node = walker.nextNode())) {
    // 각 요소에 등록된 이벤트 핸들러 확인
    const handlersMap = eventHandlers.get(node);
    if (handlersMap) {
      // 각 이벤트 타입에 대해 container를 root로 설정
      handlersMap.forEach((handlers, eventType) => {
        if (handlers && handlers.size > 0) {
          eventTypesSet.add(eventType);
        }
      });
    }
  }

  // container를 root로 설정
  let eventTypes = rootEventTypes.get(container);
  if (!eventTypes) {
    eventTypes = new Set();
    rootEventTypes.set(container, eventTypes);
  }
  eventTypesSet.forEach((eventType) => {
    eventTypes.add(eventType);
  });
}
```

- `renderElement`에서 렌더링 후 `migrateRootToContainer`를 호출하여 container의 모든 자식 요소에 등록된 이벤트 타입을 container의 root로 마이그레이션합니다.
- 이를 통해 동적으로 추가된 요소들도 container를 root로 하는 이벤트 위임 시스템에 포함됩니다.

---

### ✅ 이벤트 핸들러가 제거되면 더 이상 호출되지 않아야 한다

**구현 위치**: `src/lib/eventManager.js`

**이벤트 제거 함수**:

```107:128:src/lib/eventManager.js
export function removeEvent(element, eventType, handler) {
  if (!element || !eventType || !handler) return;

  const handlersMap = eventHandlers.get(element);
  if (!handlersMap) return;

  const handlers = handlersMap.get(eventType);
  if (!handlers) return;

  // 핸들러 제거
  handlers.delete(handler);

  // 해당 이벤트 타입의 핸들러가 없으면 맵에서 제거
  if (handlers.size === 0) {
    handlersMap.delete(eventType);
  }

  // element에 등록된 핸들러가 없으면 WeakMap에서 제거
  if (handlersMap.size === 0) {
    eventHandlers.delete(element);
  }
}
```

**Diff 알고리즘에서의 이벤트 제거**:

```17:27:src/lib/updateElement.js
    // 이벤트 핸들러 처리
    if (key.startsWith("on") && typeof oldValue === "function") {
      const eventType = key.slice(2).toLowerCase();
      removeEvent(target, eventType, oldValue);
    }

    if (key.startsWith("on") && typeof newValue === "function") {
      const eventType = key.slice(2).toLowerCase();
      addEvent(target, eventType, newValue);
      return;
    }
```

**구현 내용**:
- **핸들러 제거**: `removeEvent` 함수를 통해 특정 요소의 특정 이벤트 타입의 핸들러를 제거합니다.
- **자동 정리**: 핸들러가 모두 제거되면 해당 이벤트 타입을 맵에서 제거하고, 요소에 등록된 핸들러가 없으면 WeakMap에서도 제거합니다.
- **Diff 알고리즘 통합**: `updateElement`의 `updateAttributes` 함수에서 속성이 변경될 때 이전 핸들러를 제거하고 새로운 핸들러를 등록합니다.
- **위임 핸들러 유지**: root의 위임 핸들러는 유지되지만, 특정 요소의 핸들러가 제거되면 해당 요소에서는 더 이상 이벤트가 발생하지 않습니다.

**동작 방식**:
1. 요소의 핸들러가 제거되면 `eventHandlers` WeakMap에서 해당 핸들러를 삭제합니다.
2. 이벤트가 발생해도 위임 핸들러가 해당 요소의 핸들러를 찾지 못하므로 실행되지 않습니다.
3. 위임 핸들러 자체는 root에 남아있지만, 등록된 핸들러가 없으면 아무 동작도 하지 않습니다.

---

## Diff 알고리즘 구현

### ✅ 초기 렌더링이 올바르게 수행되어야 한다

**구현 위치**: `src/lib/renderElement.js`

```9:51:src/lib/renderElement.js
export function renderElement(vNode, container) {
  // 최초 렌더링시에는 createElement로 DOM을 생성하고
  // 이후에는 updateElement로 기존 DOM을 업데이트한다.
  // 렌더링이 완료되면 container에 이벤트를 등록한다.

  if (!container) return;

  // vNode 정규화 (컴포넌트 실행)
  const normalizedVNode = normalizeVNode(vNode);

  // 이전 vNode 가져오기
  const oldVNode = containerVNodes.get(container);

  if (!oldVNode) {
    // 최초 렌더링: createElement로 DOM 생성
    const element = createElement(normalizedVNode);

    // container 비우기
    container.innerHTML = "";

    // DOM 추가
    if (element) {
      if (element instanceof DocumentFragment) {
        container.appendChild(element);
      } else {
        container.appendChild(element);
      }
    }
  } else {
    // 업데이트: updateElement로 DOM 업데이트
    updateElement(container, normalizedVNode, oldVNode, 0);
  }

  // 이전 vNode 저장
  containerVNodes.set(container, normalizedVNode);

  // container의 모든 자식 요소에 대해 container를 root로 설정
  migrateRootToContainer(container);

  // 이벤트 리스너 설정
  // container를 root로 사용하도록 설정
  setupEventListeners(container);
}
```

**구현 내용**:
- **초기 렌더링 감지**: `containerVNodes` WeakMap을 사용하여 container에 이전 vNode가 있는지 확인합니다.
- **초기 렌더링 처리**: 이전 vNode가 없으면 `createElement`를 사용하여 전체 DOM 트리를 생성합니다.
- **vNode 저장**: 렌더링 후 현재 vNode를 저장하여 다음 렌더링에서 diff 알고리즘에 사용합니다.
- **이벤트 설정**: 렌더링 완료 후 이벤트 위임 시스템을 설정합니다.

**초기 렌더링 플로우**:
1. vNode 정규화 (`normalizeVNode`)
2. 이전 vNode 확인 → 없음
3. `createElement`로 DOM 생성
4. container에 DOM 추가
5. vNode 저장
6. 이벤트 위임 설정

---

### ✅ diff 알고리즘을 통해 변경된 부분만 업데이트해야 한다

**구현 위치**: `src/lib/updateElement.js`

**핵심 Diff 함수**:

```95:222:src/lib/updateElement.js
export function updateElement(parentElement, newNode, oldNode, index = 0) {
  if (!parentElement) return;

  const currentChild = parentElement.childNodes[index];

  // 새 노드가 없으면 기존 노드 제거
  if (!newNode || newNode === null || newNode === undefined) {
    if (currentChild) {
      parentElement.removeChild(currentChild);
    }
    return;
  }

  // 이전 노드가 없으면 새 노드 추가
  if (!oldNode || oldNode === null || oldNode === undefined) {
    const newElement = createElement(newNode);
    if (newElement) {
      if (index < parentElement.childNodes.length) {
        parentElement.insertBefore(newElement, parentElement.childNodes[index]);
      } else {
        parentElement.appendChild(newElement);
      }
    }
    return;
  }

  // 둘 다 문자열/숫자인 경우
  if (
    (typeof newNode === "string" || typeof newNode === "number") &&
    (typeof oldNode === "string" || typeof oldNode === "number")
  ) {
    if (String(newNode) !== String(oldNode)) {
      if (currentChild && currentChild.nodeType === Node.TEXT_NODE) {
        currentChild.textContent = String(newNode);
      } else {
        const textNode = document.createTextNode(String(newNode));
        if (currentChild) {
          parentElement.replaceChild(textNode, currentChild);
        } else {
          parentElement.appendChild(textNode);
        }
      }
    }
    return;
  }

  // 새 노드가 문자열/숫자이고 이전 노드가 다른 타입인 경우
  if (typeof newNode === "string" || typeof newNode === "number") {
    const textNode = document.createTextNode(String(newNode));
    if (currentChild) {
      parentElement.replaceChild(textNode, currentChild);
    } else {
      parentElement.appendChild(textNode);
    }
    return;
  }

  // 이전 노드가 문자열/숫자이고 새 노드가 다른 타입인 경우
  if (typeof oldNode === "string" || typeof oldNode === "number") {
    const newElement = createElement(newNode);
    if (newElement) {
      if (currentChild) {
        parentElement.replaceChild(newElement, currentChild);
      } else {
        parentElement.appendChild(newElement);
      }
    }
    return;
  }

  // 둘 다 VNode 객체인 경우
  if (
    typeof newNode === "object" &&
    newNode !== null &&
    "type" in newNode &&
    typeof oldNode === "object" &&
    oldNode !== null &&
    "type" in oldNode
  ) {
    // 타입이 다르면 교체
    if (newNode.type !== oldNode.type) {
      const newElement = createElement(newNode);
      if (newElement) {
        if (currentChild) {
          parentElement.replaceChild(newElement, currentChild);
        } else {
          parentElement.appendChild(newElement);
        }
      }
      return;
    }

    // 타입이 같으면 속성과 자식 업데이트
    if (currentChild && currentChild.nodeType === Node.ELEMENT_NODE) {
      // 속성 업데이트
      updateAttributes(currentChild, newNode.props, oldNode.props);

      // 자식 업데이트
      const newChildren = newNode.children || [];
      const oldChildren = oldNode.children || [];
      const maxLength = Math.max(newChildren.length, oldChildren.length);

      // 기존 자식들을 역순으로 제거 (인덱스가 변경되지 않도록)
      for (let i = maxLength - 1; i >= 0; i--) {
        if (i >= newChildren.length) {
          // 새 자식이 없으면 제거
          if (currentChild.childNodes[i]) {
            currentChild.removeChild(currentChild.childNodes[i]);
          }
        } else {
          // 자식 업데이트
          updateElement(currentChild, newChildren[i], oldChildren[i], i);
        }
      }
    }
    return;
  }

  // 그 외의 경우는 새 노드로 교체
  const newElement = createElement(newNode);
  if (newElement) {
    if (currentChild) {
      parentElement.replaceChild(newElement, currentChild);
    } else {
      parentElement.appendChild(newElement);
    }
  }
}
```

**속성 Diff 함수**:

```4:93:src/lib/updateElement.js
function updateAttributes(target, newProps, oldProps) {
  const allKeys = new Set([
    ...(newProps ? Object.keys(newProps) : []),
    ...(oldProps ? Object.keys(oldProps) : []),
  ]);

  allKeys.forEach((key) => {
    const newValue = newProps?.[key];
    const oldValue = oldProps?.[key];

    // 값이 같으면 스킵
    if (newValue === oldValue) return;

    // 이벤트 핸들러 처리
    if (key.startsWith("on") && typeof oldValue === "function") {
      const eventType = key.slice(2).toLowerCase();
      removeEvent(target, eventType, oldValue);
    }

    if (key.startsWith("on") && typeof newValue === "function") {
      const eventType = key.slice(2).toLowerCase();
      addEvent(target, eventType, newValue);
      return;
    }

    // className 처리
    if (key === "className") {
      if (newValue !== null && newValue !== undefined) {
        target.setAttribute("class", newValue || "");
      } else {
        target.removeAttribute("class");
      }
      return;
    }

    // 불리언 속성 처리
    if (typeof newValue === "boolean") {
      // checked와 selected는 DOM 속성 없이 property만 사용
      const isPropertyOnly = key === "checked" || key === "selected";

      if (newValue) {
        if (isPropertyOnly) {
          // property만 설정, DOM 속성은 설정하지 않음
          if (key in target) {
            target[key] = true;
          }
        } else {
          // 일반 boolean 속성은 DOM 속성도 설정
          target.setAttribute(key, "");
          if (key in target) {
            target[key] = true;
          }
        }
      } else {
        // false일 때는 항상 제거
        target.removeAttribute(key);
        if (key in target) {
          target[key] = false;
        }
      }
      return;
    }

    // data-* 속성 처리
    if (key.startsWith("data-")) {
      const dataKey = key
        .slice(5)
        .replace(/-([a-z])/g, (_, letter) => letter.toUpperCase());
      if (newValue !== null && newValue !== undefined) {
        target.dataset[dataKey] = newValue;
      } else {
        delete target.dataset[dataKey];
      }
      return;
    }

    // 일반 속성 처리
    if (newValue !== null && newValue !== undefined) {
      target.setAttribute(key, newValue);
      if (key in target) {
        target[key] = newValue;
      }
    } else {
      target.removeAttribute(key);
      if (key in target) {
        target[key] = null;
      }
    }
  });
}
```

**구현 내용**:
- **변경 감지**: 새 vNode와 이전 vNode를 비교하여 변경된 부분만 업데이트합니다.
- **최적화**: 값이 같으면 스킵하여 불필요한 DOM 조작을 방지합니다.
- **재귀적 처리**: 자식 노드들도 재귀적으로 diff 알고리즘을 적용합니다.
- **타입별 처리**: 문자열/숫자, VNode 객체 등 타입에 따라 적절한 업데이트 방식을 선택합니다.

**Diff 알고리즘 전략**:
1. **노드 제거**: 새 노드가 없으면 기존 노드 제거
2. **노드 추가**: 이전 노드가 없으면 새 노드 추가
3. **텍스트 노드 업데이트**: 텍스트 내용만 변경되면 `textContent`만 업데이트
4. **요소 재사용**: 타입이 같으면 기존 요소를 재사용하고 속성과 자식만 업데이트
5. **요소 교체**: 타입이 다르면 새 요소로 교체

---

### ✅ 새로운 요소를 추가하고 불필요한 요소를 제거해야 한다

**구현 내용**:

**새 요소 추가**:
```108:119:src/lib/updateElement.js
  // 이전 노드가 없으면 새 노드 추가
  if (!oldNode || oldNode === null || oldNode === undefined) {
    const newElement = createElement(newNode);
    if (newElement) {
      if (index < parentElement.childNodes.length) {
        parentElement.insertBefore(newElement, parentElement.childNodes[index]);
      } else {
        parentElement.appendChild(newElement);
      }
    }
    return;
  }
```

**불필요한 요소 제거**:
```100:106:src/lib/updateElement.js
  // 새 노드가 없으면 기존 노드 제거
  if (!newNode || newNode === null || newNode === undefined) {
    if (currentChild) {
      parentElement.removeChild(currentChild);
    }
    return;
  }
```

**자식 요소 추가/제거**:
```192:208:src/lib/updateElement.js
      // 자식 업데이트
      const newChildren = newNode.children || [];
      const oldChildren = oldNode.children || [];
      const maxLength = Math.max(newChildren.length, oldChildren.length);

      // 기존 자식들을 역순으로 제거 (인덱스가 변경되지 않도록)
      for (let i = maxLength - 1; i >= 0; i--) {
        if (i >= newChildren.length) {
          // 새 자식이 없으면 제거
          if (currentChild.childNodes[i]) {
            currentChild.removeChild(currentChild.childNodes[i]);
          }
        } else {
          // 자식 업데이트
          updateElement(currentChild, newChildren[i], oldChildren[i], i);
        }
      }
```

**구현 내용**:
- **새 요소 추가**: 이전 노드가 없고 새 노드가 있으면 `createElement`로 생성하여 적절한 위치에 추가합니다.
- **요소 제거**: 새 노드가 없고 이전 노드가 있으면 해당 DOM 요소를 제거합니다.
- **자식 요소 관리**: 자식 배열을 비교하여:
  - 새 자식이 추가되면 재귀적으로 `updateElement` 호출 (이전 노드가 없으므로 추가됨)
  - 기존 자식이 제거되면 `removeChild`로 제거
  - 역순으로 처리하여 인덱스 변경 문제를 방지합니다.

---

### ✅ 요소의 속성만 변경되었을 때 요소를 재사용해야 한다

**구현 내용**:

```165:210:src/lib/updateElement.js
  // 둘 다 VNode 객체인 경우
  if (
    typeof newNode === "object" &&
    newNode !== null &&
    "type" in newNode &&
    typeof oldNode === "object" &&
    oldNode !== null &&
    "type" in oldNode
  ) {
    // 타입이 다르면 교체
    if (newNode.type !== oldNode.type) {
      const newElement = createElement(newNode);
      if (newElement) {
        if (currentChild) {
          parentElement.replaceChild(newElement, currentChild);
        } else {
          parentElement.appendChild(newElement);
        }
      }
      return;
    }

    // 타입이 같으면 속성과 자식 업데이트
    if (currentChild && currentChild.nodeType === Node.ELEMENT_NODE) {
      // 속성 업데이트
      updateAttributes(currentChild, newNode.props, oldNode.props);

      // 자식 업데이트
      const newChildren = newNode.children || [];
      const oldChildren = oldNode.children || [];
      const maxLength = Math.max(newChildren.length, oldChildren.length);

      // 기존 자식들을 역순으로 제거 (인덱스가 변경되지 않도록)
      for (let i = maxLength - 1; i >= 0; i--) {
        if (i >= newChildren.length) {
          // 새 자식이 없으면 제거
          if (currentChild.childNodes[i]) {
            currentChild.removeChild(currentChild.childNodes[i]);
          }
        } else {
          // 자식 업데이트
          updateElement(currentChild, newChildren[i], oldChildren[i], i);
        }
      }
    }
    return;
  }
```

**속성 업데이트 최적화**:

```4:15:src/lib/updateElement.js
function updateAttributes(target, newProps, oldProps) {
  const allKeys = new Set([
    ...(newProps ? Object.keys(newProps) : []),
    ...(oldProps ? Object.keys(oldProps) : []),
  ]);

  allKeys.forEach((key) => {
    const newValue = newProps?.[key];
    const oldValue = oldProps?.[key];

    // 값이 같으면 스킵
    if (newValue === oldValue) return;
```

**구현 내용**:
- **타입 비교**: 새 노드와 이전 노드의 `type`이 같으면 기존 DOM 요소를 재사용합니다.
- **속성만 업데이트**: `updateAttributes` 함수를 통해 변경된 속성만 업데이트합니다.
- **최적화**: 속성 값이 같으면 스킵하여 불필요한 DOM 조작을 방지합니다.
- **요소 재사용**: DOM 요소를 재사용하므로 이벤트 리스너, 포커스 상태 등이 유지됩니다.

**예시**:
```jsx
// 이전: <div className="old">Content</div>
// 새:   <div className="new">Content</div>
// → 같은 div 요소를 재사용하고 className만 변경
```

---

### ✅ 요소의 타입이 변경되었을 때 새로운 요소를 생성해야 한다

**구현 내용**:

```174:185:src/lib/updateElement.js
    // 타입이 다르면 교체
    if (newNode.type !== oldNode.type) {
      const newElement = createElement(newNode);
      if (newElement) {
        if (currentChild) {
          parentElement.replaceChild(newElement, currentChild);
        } else {
          parentElement.appendChild(newElement);
        }
      }
      return;
    }
```

**구현 내용**:
- **타입 변경 감지**: 새 노드와 이전 노드의 `type`이 다르면 완전히 새로운 요소를 생성합니다.
- **요소 교체**: `replaceChild`를 사용하여 기존 요소를 새 요소로 교체합니다.
- **완전 교체**: 타입이 다르면 속성 비교 없이 바로 교체하므로, 불필요한 비교를 생략합니다.

**예시**:
```jsx
// 이전: <div>Content</div>
// 새:   <span>Content</span>
// → div 요소를 제거하고 span 요소를 새로 생성하여 교체
```

**타입 변경 시나리오**:
1. HTML 태그 변경: `div` → `span`
2. 컴포넌트 변경: `Button` → `Link`
3. 텍스트 노드 ↔ 요소 노드: `"text"` → `<div>text</div>`

---

## 요약

### 구현 완료 상태

| 요구사항 | 상태 | 구현 위치 |
|---------|------|----------|
| createVNode 함수 | ✅ | `src/lib/createVNode.js` |
| normalizeVNode 함수 | ✅ | `src/lib/normalizeVNode.js` |
| createElement 함수 | ✅ | `src/lib/createElement.js` |
| JSX → DOM 변환 | ✅ | `src/render.jsx`, `src/lib/renderElement.js` |
| 이벤트 위임 | ✅ | `src/lib/eventManager.js` |
| 동적 요소 이벤트 | ✅ | `src/lib/eventManager.js` |
| 이벤트 핸들러 제거 | ✅ | `src/lib/eventManager.js`, `src/lib/updateElement.js` |
| 초기 렌더링 | ✅ | `src/lib/renderElement.js` |
| Diff 알고리즘 | ✅ | `src/lib/updateElement.js` |
| 요소 추가/제거 | ✅ | `src/lib/updateElement.js` |
| 속성 변경 시 재사용 | ✅ | `src/lib/updateElement.js` |
| 타입 변경 시 교체 | ✅ | `src/lib/updateElement.js` |

### 전체 아키텍처

```
JSX 작성
  ↓
createVNode (vNode 생성)
  ↓
normalizeVNode (정규화 및 컴포넌트 실행)
  ↓
renderElement
  ├─ 초기 렌더링: createElement → DOM 생성
  └─ 업데이트: updateElement (Diff 알고리즘)
      ├─ 타입 비교
      ├─ 속성 업데이트 (updateAttributes)
      └─ 자식 재귀 업데이트
  ↓
이벤트 위임 설정 (setupEventListeners)
```

모든 요구사항이 완전히 구현되어 있으며, 가상돔 기반의 효율적인 렌더링 시스템이 구축되어 있습니다.

